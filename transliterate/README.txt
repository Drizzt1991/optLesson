Tasks:
    - Write library to perform char transliteration for cyrilic symbols.

Motivation: Transliterate cyrilic names to ascii. 
Usage: URL's is a good example.

Implementations:
    - As a pure python function
    - As a pure python class
    - As pure C extension as a function
    - As pure C extension as a class
    - In Cython as a class
    - As pure C lib with C bindings using ctypes and cffy
    - As a C++ lib with Boost Python bindings
